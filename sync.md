# Post-Sync Actions

## Dependencies

```sh
cd tauri-app && npm install && cd ..
```

## Notifications

- `CLAUDE.md` — review if project conventions were updated
- `SPEC.md` — check for spec changes
- `tauri-app/src-tauri/Cargo.toml` — Rust dependency changes
- `tauri-app/package.json` — frontend dependency changes
